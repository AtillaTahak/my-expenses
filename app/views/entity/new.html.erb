
  <div class="nav-header">
    <div>
        <%= link_to ('<i class="fa fa-arrow-left"></i>').html_safe, :back, { 'class': 'arrow'} %>
    </div>
    <h4 class="text-gray-4">NEW Expenses </h4>
    <div class="actions">
  <%= button_to 'LOG OUT', destroy_user_session_path, method: :delete, class: 'btn btn-primary' %>
    </div>
  </div>


  <% if @entity.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@entity.errors.count, "error") %> prohibited this Expenses from being saved:</h2>

      <ul>
        <% @entity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<%= form_with(model: @entity,  url:"/users/#{current_user.id}/group/#{@group_id}/entity" ) do |form| %>

 <div class= "form-group-custom" >
      <%= form.text_field :name,   class:"form-control-custom", placeholder: "Enter Expenses name", autofocus: true %>
    </div>

 <div class= "form-group-custom" >
      <%= form.number_field :amount,   class:"form-control-custom", placeholder: "Enter Expenses amount", autofocus: true %>
  </div>

  <div class= "form-group d-flex" >
  
  <div class='add-ne-category-btn-container-main'>
    <div class= 'add-ne-category-btn-container'>
        <%=  form.submit ('SUBMIT'), class:" add-ne-category-btn text-gray-4" %>
      </div> 
    </div> 
  </div> 
<% end %>