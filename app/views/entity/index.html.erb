
<div class="nav-header ">
 <div>
   <%= link_to ('<i class="fa fa-arrow-left"></i>').html_safe, :back, { 'class': 'arrow'} %>

    </div>
  <h4 class="text-gray-4 ">Expenses</h4> 
  <div> </div>
</div>

<div class= "expenditures-total">
  <div class="icon-container"> 
  </div>
<% if @entities.count > 0 %>
  <div class=" flex-column">
    <p> <strong>Category:</strong> <%= @group_id.name %> </p>
    <p><strong>Total:</strong> <%= @group_id.total_funds %> </p>
  </div>
  <%end %>
</div>

<div id="expenditures">
  <% @entities.each do |entity| %>
    <p>
     <div id="<%= dom_id entity %>">

<div class= 'category-container'>
<div><strong><%= @group_id.name %>:</strong> </div>
  <div> <%= entity.name %> </div>
  <div>$ <%= entity.amount %></div>
</div>
</div>
    </p>
  <% end %>
</div>
<div class='add-ne-category-btn-container-main'>
  <div class= 'add-ne-category-btn-container'>
    <%= link_to "Add a new expenditure", new_user_group_entity_path(user_id: current_user.id, group_id: @group_id), class: 'add-ne-category-btn text-gray-4' %>
  </div> 
</div> 